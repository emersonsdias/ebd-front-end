import { EnumTranslatePipe } from './enum-translate.pipe';

describe('EnumTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
